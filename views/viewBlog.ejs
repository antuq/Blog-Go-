<%- include("./partials/header.ejs") %>

<section class="container d-flex justify-content-center my-5">
    <div class="row">
        <div class="col-12 text-center">
            <h1><%= blog.title %></h1>
            <img src="/uploads/<%= blog.banner %>" alt="Banner Image" class="img-fluid mb-4">
            <p><%= blog.description %></p>
            <p><small>Posted on <%= new Date(blog.date).toLocaleDateString() %></small></p>
        </div>
    </div>
</section>

<%- include("./partials/footer.ejs") %>
